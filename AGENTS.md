這是 React 裡的連線層，只處理一條 WebSocket 的生命週期。公開介面與預設維持現狀。重連、指數退避、隨機抖動、liveness 都屬於這層。不擁有訊息協定。沒有被提出來的能力不是缺陷，也不要因為別的套件有而加。

不要把現在的程式或測試當成標準。現有行為若讓連線 silently 壞掉，依連線層常見做法修正。有多種做法時，選不新增 API、也不改預設的那一種，並在清單寫明選了哪一種、為什麼。
